import 'package:flutter/material.dart';

extension BurderRadiusStyles on BorderRadius {
  static BorderRadius large = const BorderRadius.all(Radius.circular(12));
  static BorderRadius normal = const BorderRadius.all(Radius.circular(8));
}

class AppPadding {
  static const double normal = 12;
  static const double small = 12;
}
